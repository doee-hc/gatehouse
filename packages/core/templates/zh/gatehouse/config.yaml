schema_version: 1
locale: zh
portal:
  # admin_key 在首次初始化或启动时自动生成，用于 /admin 频道管理解锁
  # display:
  #   sse_max: 500
  #   snapshot_ttl_ms: 5000
  #   team_stats_ttl_ms: 10000
  #   blog_ttl_ms: 30000
  #   cors_origins:
  #     - https://portal.example.com
  #   snapshot_poll_ms: 15000
  #   team_stats_poll_ms: 12000
  # office:
  #   idle_wander: true
  #   play_release: seat   # seat | wander — 地板点击彩蛋结束后
  brand:
    title: Gatehouse
    subtitle: 团队门户
    logo: brand/logo.png
    # icp_text: ""
agents:
  lead:
    name: Len
  architect:
    name: Archie
  curator:
    name: Kurt
  arbiter:
    name: Art
# 调试 session 上下文（仅开发者 CLI，运行时不会读取）:
# bunx @gatehouse/core dump-context -C . --mission <mission_id>
# Optional per-role models (provider/model-id). Project overrides ~/.config/gatehouse/config.yaml.
# models:
#   lead: opencode/big-pickle
#   architect: opencode/big-pickle
#   curator: opencode/deepseek-v4-flash-free
#   arbiter: opencode/deepseek-v4-flash-free
#   executor: opencode/big-pickle
# 看门狗时间（毫秒）。顶层字段为 execution / record 共用默认值；各子项可单独覆盖。
# 修改后需重启 OpenCode 生效。项目 .gatehouse/config.yaml 覆盖 ~/.config/gatehouse/config.yaml。
# watchdog:
#   poll_ms: 2000
#   idle_threshold_ms: 10000
#   wake_cooldown_ms: 30000
#   execution: {}
#   record: {}
#   orchestration_stall:
#     stall_threshold_ms: 180000
#     notify_cooldown_ms: 300000
#     resume_cooldown_ms: 60000
#   autopilot:
#     poll_ms: 30000
#     idle_threshold_ms: 180000
#     wake_cooldown_ms: 30000
