# Retro record watchdog

Mission **{{mission_id}}** 的 retro-analyst session 已空闲 **{{idle_seconds}}** 秒，尚未调用 `gatehouse_retro_record`。

## 请立即完成

1. 确认 retro-summary 已写入：`{{report_path}}`。
2. 调用 **`gatehouse_retro_record()`** 登记完成。
