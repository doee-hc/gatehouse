# 子树交付索引 · <node_id>

> 协调层 / 任务协调者：**禁止**抄写下属 `*-delivery.md` 正文。只列路径、一句话状态；本节点自有工作（如有）单独一小节。

## 直接下属交付

| node_id | 报告路径 | 状态 |
|---------|----------|------|
| `<child>` | `.gatehouse/trees/<mission_id>/reports/nodes/<child>-delivery.md` | 已收到 / 待返工 |

## 本节点自有工作

（协调者自己动手完成的部分；无则写「无」）
