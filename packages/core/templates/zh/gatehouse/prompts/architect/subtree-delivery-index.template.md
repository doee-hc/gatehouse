# 子树汇总 · <node_id>

> 协调层：汇总下属汇报时**只列路径与一句话状态**；本节点自有工作（如有）单独一小节。**禁止**抄写下属 artifact 正文。

## 直接下属

| node_id | 状态 |
|---------|------|
| `<child>` | 已收到 / 待返工 |

## 本节点自有工作

（无则写「无」）

完成本波次后：`gatehouse_execution_complete(summary=..., artifacts=?)`。
