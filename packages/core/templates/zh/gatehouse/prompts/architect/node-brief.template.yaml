node_id: <node_id>
role: 执行成员
your_work:
  - 本节点具体产出（行动依据）
not_your_job:
  - 非本节点职责（勿扩大范围；并列 sibling 的职责写在此，避免内容重叠）
acceptance_slice:
  - path: <node_id>/
  - path: reports/<node_id>/
  - 只交付本节点 path；勿 read sibling 节点 artifact
