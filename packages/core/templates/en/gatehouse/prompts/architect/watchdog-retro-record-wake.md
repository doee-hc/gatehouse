# Retro record watchdog

Mission **{{mission_id}}** node **{{node_id}}** retro session has been idle for **{{idle_seconds}}** seconds without calling `gatehouse_retro_record`.

## Complete now

1. Confirm the retro report is written: `{{report_path}}` (include "Tool contribution" section).
2. Call **`gatehouse_retro_record()`** (or `report_path=` pointing to the report).
