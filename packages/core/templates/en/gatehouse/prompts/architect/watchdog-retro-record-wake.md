# Retro record watchdog

Mission **{{mission_id}}** retro-analyst session has been idle for **{{idle_seconds}}** seconds without calling `gatehouse_retro_record`.

## Complete now

1. Confirm retro-summary is written: `{{report_path}}`.
2. Call **`gatehouse_retro_record()`** to register completion.
