node_id: <node_id>
role: execution member
your_work:
  - Concrete deliverables for this node
not_your_job:
  - Out of scope (list sibling boundaries to avoid overlap)
acceptance_slice:
  - path: <node_id>/
  - path: reports/<node_id>/
  - Deliver only under these paths; do not read sibling artifacts
