schema_version: 1
locale: en
portal:
  # admin_key is auto-generated on first scaffold / start for /admin channel unlock
  # display:
  #   sse_max: 500
  #   snapshot_ttl_ms: 5000
  #   team_stats_ttl_ms: 10000
  #   blog_ttl_ms: 30000
  #   cors_origins:
  #     - https://portal.example.com
  #   snapshot_poll_ms: 15000
  #   team_stats_poll_ms: 12000
  # office:
  #   idle_wander: true
  #   play_release: seat   # seat | wander — after floor-click easter egg
  brand:
    title: Gatehouse
    subtitle: Team Portal
    logo: brand/logo.png
    # icp_text: ""
agents:
  lead:
    name: Len
  architect:
    name: Archie
  curator:
    name: Kurt
  arbiter:
    name: Art
# Debug session context (developer CLI only; runtime never reads dumps):
# bunx @gatehouse/core dump-context -C . --mission <mission_id>
# Optional per-role models (provider/model-id). Project overrides ~/.config/gatehouse/config.yaml.
# models:
#   lead: opencode/big-pickle
#   architect: opencode/big-pickle
#   curator: opencode/deepseek-v4-flash-free
#   arbiter: opencode/deepseek-v4-flash-free
#   executor: opencode/big-pickle
#   coordinator: opencode/big-pickle
