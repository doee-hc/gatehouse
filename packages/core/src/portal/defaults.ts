/** Portal display port (UI + read API). Chosen to avoid 8787/8788 (common MCP / IDE tooling). */
export const DEFAULT_PORTAL_DISPLAY_PORT = 18471

/** Portal admin port (/admin, channels). Display port + 1. */
export const DEFAULT_PORTAL_ADMIN_PORT = 18472
